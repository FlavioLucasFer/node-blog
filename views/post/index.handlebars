<div class="card">
	<div class="card-body">
		<h1>{{post.title}}</h1>
		
		<hr>
		<small>Category: {{post.category}}</small>
		<br>
		<small>Posted at: {{post.date}}</small>
		<hr>
		
		<p>{{post.contents}}</p>
	</div>
</div>